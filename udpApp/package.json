{
  "name": "udp-app",
  "version": "1.0.0",
  "private": true,
  "description": "UDP+SSE服务 + 静态网页",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:linux": "electron-builder --linux --config build-web-only.yml",
    "build:linuxUDP": "electron-builder --linux --config build-with-server.yml"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "^24.9.1"
  }
}
